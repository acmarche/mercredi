{% extends '@AcMarcheMercrediEcole/layout.html.twig' %}

{% block body %}
    <div class="card mb-3">

        <div class="card-header">
            <h3 class="w-50 d-inline">Liste des {{ enfants|length}} enfants</h3>
        </div>

        <div class="card-body">

            {% if mercredi_accueil > 1 %}
                <div class="alert alert-success">
                    La liste ci-dessous affiche les enfants qui <strong>viennent au matin et/ou au soir.</strong><br/>
                    Pour afficher les enfants, qui ne sont <strong>pas encore inscrits</strong> décochez la case et
                    cliquez
                    sur rechercher.
                </div>
            {% endif %}

            {% include '@AcMarcheMercrediEcole/enfant/_search.html.twig' %}
            {% include '@AcMarcheMercrediEcole/enfant/_list.html.twig' %}
        </div>

    </div>
{% endblock %}
