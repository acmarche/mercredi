<div class="d-grid gap-2 d-md-block mb-3">

    {% if mercredi_accueil > 1 %}
        {% include '@AcMarcheMercredi/ecole/enfant/_btn_add_accueil.html.twig' %}
    {% endif %}

    <a href="{{ path('mercredi_ecole_sante_fiche_show', { 'uuid': enfant.uuid }) }}" role="button"
       class="btn btn-primary"><i class="fas fa-heartbeat"></i> Fiche santé</a>

    {% if mercredi_accueil > 1 %}
        <a href="{{ path('mercredi_ecole_accueil_retard', { 'uuid': enfant.uuid }) }}" role="button"
           class="btn btn-primary"><i class="fas fa-hourglass-half"></i> Retard</a>
    {% endif %}

    <a href="{{ path('mercredi_ecole_enfant_index') }}" class="btn btn-primary" role='button'><i
                class="fas fa-list"></i>
        Retour à la Liste</a>
</div>
