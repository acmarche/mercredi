{% extends '@AcMarcheMercrediAdmin/layout.html.twig' %}

{% block body %}

    <div class="card bg-light mb-3">
        <div class="card-header">
            <h3>{{ animateur }}</h3>
        </div>
        <div class="card-body">

            {{ include('@AcMarcheMercrediAdmin/animateur/_form.html.twig') }}

            <a href="{{ path('mercredi_admin_animateur_show',{'id':animateur.id}) }}" class="mt-4">
                <i class="fas fa-long-arrow-alt-left"></i> Annuler
            </a>
        </div>
    </div>

{% endblock %}
