<a href="{{ path('mercredi_admin_page_edit', { 'id': page.id }) }}" role="button"
   class="btn btn-primary"><i
            class="far fa-edit"></i> Modifier</a>

<a href="{{ path('mercredi_admin_document_new_from_page', { 'id': page.id }) }}" role="button"
   class="btn btn-primary"><i
            class="fas fa-plus"></i> Ajouter un document</a>

<a href="{{ path('mercredi_front_page_show', { 'slug': page.slug }) }}" role="button"
   class="btn btn-primary"><i
            class="fas fa-eye"></i> Voir sur le site</a>

<a href="{{ path('mercredi_admin_page_index') }}" class="btn btn-primary" role='button'><i class="fas fa-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">
    <i class="fas fa-trash"></i> Supprimer
</button>

<br /><br />
