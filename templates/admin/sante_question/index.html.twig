{% extends '@AcMarcheMercrediAdmin/layout.html.twig' %}

{% block body %}
    <h3>Liste des questions</h3>

    <a class="btn btn-primary m-2" role="button"
       href="{{ path('mercredi_admin_sante_question_new') }}"><i class="fas fa-plus"></i> Ajouter une question</a>
 <a class="btn btn-primary m-2" role="button"
       href="{{ path('mercredi_admin_sante_question_sort') }}"><i class="fas fa-sort"></i> Ordre d'affichage</a>

    <table class="table table-bordered table-hover">
        <thead>
        <tr>
            <th>Intitulé</th>
            <th>Complément d'information</th>
        </tr>
        </thead>
        <tbody>
        {% for sante_question in sante_questions %}
            <tr>
                <td>
                    <a href="{{ path('mercredi_admin_sante_question_show', { 'id': sante_question.id }) }}">
                        {{ sante_question.nom }}
                    </a>
                </td>
                <td>{{ sante_question.complementLabel }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
