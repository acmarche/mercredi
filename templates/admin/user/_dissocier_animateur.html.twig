<!-- Modal candidat -->
<div class="modal fade" id="dissocierModalAnimateur" tabindex="-1" role="dialog" aria-labelledby="dissocierModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Dissocier</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Dissocier l' animateur du compte ?
            </div>
            <div class="modal-footer">
                <form method="post" action="{{ path('mercredi_user_dissociate_animateur', {'id': user.id}) }}">

                    <input type="hidden" name="_token" value="{{ csrf_token('dissociate' ~ user.id) }}">
                    <input type="hidden" name="animateur" id="modal-animateur" value="">
                    <button type="button" class="btn btn-secondary"
                            data-dismiss="modal">Annuler
                    </button>
                    <button type="submit" class="btn btn-danger">Dissocier l' animateur</button>
                </form>
            </div>
        </div>
    </div>
</div>
