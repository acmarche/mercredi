{{ form_start(form) }}
{{ form_errors(form) }}
{{ form_row(form.nom) }}
<div class="row">
    <div class="col">{{ form_row(form.prix1) }}</div>
    <div class="col">{{ form_row(form.prix2) }}</div>
    <div class="col">{{ form_row(form.prix3) }}</div>
</div>
{{ form_row(form.plaine_groupes) }}
{{ form_row(form.prematernelle) }}
{{ form_row(form.communication) }}
{{ form_row(form.remarque) }}
<button class="btn btn-primary mr-1 mb-2" type="submit"><i class="far fa-save" aria-hidden="true"></i>
    Sauvegarder
</button>
{{ form_end(form) }}
