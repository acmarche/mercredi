{% extends '@AcMarcheMercrediAdmin/layout.html.twig' %}

{% block body %}

<div class="card bg-light mb-3">
    <div class="card-header">
        <h3>{{ utilisateur }}</h3>
    </div>
    <div class="card-body">

    {% include '@AcMarcheMercrediAdmin/utilisateur/_actions.html.twig' %}

    <br />
    <table class="table table-bordered table-hover">
        <tbody>
        <tr>
            <th>Nom prénom</th>
            <td>
                {{ utilisateur.nom }} {{ utilisateur.prenom }}
            </td>
        </tr>
        <tr>
            <th>Email</th>
            <td>
                {{ utilisateur.email }}
            </td>
        </tr>
        </tbody>
    </table>
    </div>
</div>

    {% include '@AcMarcheMercrediAdmin/utilisateur/_delete.html.twig' %}

{% endblock %}
