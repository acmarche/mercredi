{% extends '@AcMarcheBottin/layout.html.twig' %}

{% block title %}{{ user }}{% endblock %}

{% block body -%}

    <div class="card bg-light mb-3">
        <div class="card-header">
            <h3>Changer le mot de passe</h3>
        </div>
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_errors(form) }}
            <button class="btn btn-primary mr-1 mb-2" type="submit"><i class="far fa-save" aria-hidden="true"></i>
                Sauvegarder
            </button>
            {{ form_end(form) }}


            <a href="{{ path('mercredi_utilisateur') }}">
                <span class="fas fa-list"></span> Annuler et retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
