<!-- Modal candidat -->
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel"
     aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Supprimer ?</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">

                </button>
            </div>
            <div class="modal-body">
                Supprimer la présence de {{ enfant}} le {{ presence.jour.dateJour | format_date('full') }} ?
            </div>
            <div class="modal-footer">
                <form method="post" action="{{ path('mercredi_parent_presence_delete', {'id': presence.id}) }}">

                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presence.id) }}">
                    <button type="button" class="btn btn-secondary"
                            data-bs-dismiss="modal">Annuler
                    </button>
                    <button type="submit" class="btn btn-danger">Supprimer la présence</button>
                </form>
            </div>
        </div>
    </div>
</div>
