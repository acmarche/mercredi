{% extends '@AcMarcheMercrediParent/layout.html.twig' %}

{% block body %}

    <div class="card bg-lig2ht mb-3">
        <div class="card-header">
            <h3>Modification de mes coordonnées</h3>
        </div>

        <div class="card-body">
            {% include '@AcMarcheMercrediParent/tuteur/_form.html.twig' %}

 </div>
        <div class="card-footer">
            <a href="{{ path('mercredi_parent_tuteur_show') }}">
                <p>
                    <span class="fas fa-list"></span> Annuler
                </p>
            </a>
        </div>
    </div>

{% endblock %}
