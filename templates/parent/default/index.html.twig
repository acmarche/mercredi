{% extends '@AcMarcheMercrediParent/layout.html.twig' %}

{% block body -%}

    <div class="card bg-lig2ht mb-3">
        <div class="card-header">
            <h3>Votre (Vos) enfant(s)</h3>
        </div>
        <div class="card-body">
            {% include '@AcMarcheMercrediParent/enfant/_list.html.twig' %}
        </div>
    </div>

    <div class="card bg-lig2ht mb-3">
        <div class="card-header">

            <h3 class="w-50 d-inline">Vos cooronnées</h3>
            <a href="{{ path('mercredi_parent_tuteur_edit') }}" class="float-right" role="button">
                <i class="far fa-edit"></i>
                Modifier
            </a>
        </div>
        <div class="card-body">
            {% include '@AcMarcheMercrediParent/tuteur/_coordonnees.html.twig' %}
        </div>
    </div>

{% endblock %}
